
set-option -g update-environment "PATH PYENV_ROOT PYENV_VERSION"
set -g default-terminal "screen-256color"
set -g prefix C-]
set -g mouse on
unbind C-b
bind-key C-] send-prefix

# remap vertical split to C-a |. pipe is more intuitive
unbind %
bind | split-window -h
# remap horizontal split to C-a -. hyphen is more intuitive
unbind '"'
bind - split-window -v

# bind tmux config refresh to C-a r
unbind r
bind r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded"

# add bindings to resize panes
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r H resize-pane -L 5
bind -r L resize-pane -R 5

# add binding to mazimize a pane
bind -r m resize-pane -Z

# handy for using the mouse to resize panes
# set -g mouse off

# plugin manager
set -g @plugin 'tmux-plugins/tpm'

# tmux plugins
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# tmux-resurrect settings
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session'

# tmux-continuum settings
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'

# settings for yazi
# https://yazi-rs.github.io/docs/image-preview/#tmux-users
# need to have at least version 3.3 of tmux which isn't in Ubunut until
# Noble numbat or later
#set -g allow-passthrough all
#set -ga update-environment TERM
#set -ga update-environment TERM_PROGRAM
set-option -sg escape-time 10
set-option -g focus-events on

# vi mode copying
set-window-option -g mode-keys vi
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# keep this init at the bottom
run  '~/.tmux/plugins/tpm/tpm'

# for neovim colors
set -as terminal-features ',*:RGB'

# suggested for sesh
bind-key x kill-pane # skip "kill-pane 1? (y/n)" prompt
set -g detach-on-destroy off  # don't exit from tmux when closing a session

# interactive sesh session selector
bind-key "t" run-shell "sesh connect \"$(
	sesh list | fzf-tmux -p 55%,60% \
		--no-sort --reverse --ansi --border-label ' sesh ' --prompt '⚡  ' \
		--header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
		--bind 'tab:down,btab:up' \
		--bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
		--bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
		--bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
		--bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
		--bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
		--bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
)\""

# dev setup keybinding - select a project and create dev environment
bind-key "T" run-shell "bash -c '
	project=\$(find ~/projects -mindepth 1 -maxdepth 1 -type d | fzf-tmux -p 55%,60% --reverse --border-label \" select project \" --prompt \"🚀  \")
	[ -z \"\$project\" ] && exit 0
	session_name=\$(basename \"\$project\")

	# Check if session exists
	if ! tmux has-session -t \"\$session_name\" 2>/dev/null; then
		# Create new session detached in the project directory
		tmux new-session -d -s \"\$session_name\" -c \"\$project\" -n \"editor\"
		tmux send-keys -t \"\$session_name:editor\" \"nv\" C-m
		tmux new-window -t \"\$session_name\" -n \"claude\" -c \"\$project\"
		tmux send-keys -t \"\$session_name:claude\" \"claude\" C-m
		tmux select-window -t \"\$session_name:editor\"
	fi

	# Switch to the session
	tmux switch-client -t \"\$session_name\"
'"




# global sessions
# bind-key "K" display-popup -h 90% -w 50% -E "sesh ui"

# bind-key "k" display-popup -E -w 40% "sesh connect \"$(
#   sesh list -i | gum filter --limit 1 --placeholder 'Pick a sesh' --prompt='⚡'
# )\""
#
# bind-key "k" display-popup -h 100% -w 100% -E "bash -lc '
#   sel=\$(sesh list --icons --hide-duplicates | fzf --no-border \
#     --list-border \
#     --no-sort --prompt \"⚡  \" \
#     --input-border \
#     --header-border \
#     --bind \"tab:down,btab:up\" \
#     --bind \"ctrl-b:abort\" \
#     --bind \"ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)\" \
#     --bind \"ctrl-t:change-prompt(  )+reload(sesh list -t --icons)\" \
#     --bind \"ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)\" \
#     --bind \"ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)\" \
#     --bind \"ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)\" \
#     --bind \"ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)\" \
#     --preview-window \"right:70%\" \
#     --preview \"sesh preview {}\") || exit 0
#   [ -n \"\$sel\" ] && sesh connect \"\$sel\"
# '"
#
# bind-key "n" display-popup -E "sesh ui"
#
# # root sessions
# bind-key "r" display-popup -h 100% -w 100% -E "bash -lc '
#   root=\$(sesh root 2>/dev/null || true)
#   sel=\$(sesh list --icons | fzf --no-border \
#     --query \"\$root\" \
#     --list-border \
#     --no-sort --prompt \"⚡  \" \
#     --input-border \
#     --bind \"tab:down,btab:up\" \
#     --bind \"ctrl-b:abort\" \
#     --bind \"ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)\" \
#     --bind \"ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)\" \
#     --bind \"ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)\" \
#     --bind \"ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)\" \
#     --bind \"ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)\" \
#     --preview-window \"right:70%\" \
#     --preview \"sesh preview {}\") || exit 0
#   [ -n \"\$sel\" ] && sesh connect \"\$sel\"
# '"
